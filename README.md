# Mood-Based Movie Recommendation System

**Course:** Artificial Intelligence (Session 202605)
**Programme:** RSW2Y2S2 | Tutorial Group: 2
**Tutor:** Ms Chai Foong Theng

---

## Project Overview

This project implements a mood-based movie recommendation system. Users select their current mood (e.g., Happy, Sad, Excited), and the system recommends movies that match both their mood and preferences. The project compares three different recommender-system approaches on the same problem.

### Team

| No. | Student | Student ID | Module |
|---|---|---|---|
| 1 | Tay Ernest | 2501307 | Collaborative Filtering |
| 2 | Chay Qian | 2501538 | Content-Based Filtering |
| 3 | — | — | Hybrid / Other |

### Deadline

- **Documentation + Prototype:** 28 August 2026 (Friday, before 12:00 PM)
- **Demo + Q&A:** Weeks 12–14

---

## Project Structure

```
mood-recommendation-system/
│
├── data/
│   ├── raw/                          # Original downloaded dataset (DO NOT MODIFY)
│   │   ├── tmdb_movie_dataset.csv
│   │   ├── tmdb_movie_ratings.csv
│   │   └── tmdb_movie_credits.csv
│   │
│   └── processed/                    # Cleaned data (generated by notebook 02)
│       ├── movies_clean.csv
│       ├── ratings_clean.csv
│       └── movie_lookup.csv
│
├── notebook/
│   ├── 01_dataset_exploration.ipynb  # Inspect raw data
│   ├── 02_data_preprocessing.ipynb   # Clean and prepare data
│   └── exploration_old.ipynb         # (original draft, kept for reference)
│
├── src/                              # Algorithm implementations (TBD)
│
├── assignment_context.md             # Full assignment context for AI assistants
├── workflow.md                       # High-level workflow steps
└── README.md                         # This file
```

---

## Dataset

**Source:** TMDB 5000 Movie Dataset with Ratings
**Origin:** [Kaggle — aayushsoni4/tmdb-5000-movie-dataset-with-ratings](https://www.kaggle.com/datasets/aayushsoni4/tmdb-5000-movie-dataset-with-ratings)

> **Note:** The raw dataset files are not included in this repository (too large for GitHub). Download them from Kaggle and place them in `data/raw/` before running the notebooks.

### Raw Files

| File | Rows | Columns | Description |
|---|---|---|---|
| `tmdb_movie_dataset.csv` | 4,602 | 21 | Movie metadata (title, genres, keywords, budget, revenue, etc.) |
| `tmdb_movie_ratings.csv` | 17,258,140 | 4 | User ratings (userId, ratingId, rating, timestamp) |
| `tmdb_movie_credits.csv` | 4,602 | 4 | Cast and crew info (tmdbId, title, cast, crew) |

### Key Fields

| Field | Table(s) | Purpose |
|---|---|---|
| `ratingId` | movies, ratings | **Primary link** between movies and ratings |
| `tmdbId` | movies, credits | Links movies to cast/crew information |
| `userId` | ratings | Identifies each user |
| `rating` | ratings | User's rating (0.5 to 5.0) |
| `genres` | movies | JSON array of genre objects |
| `keywords` | movies | JSON array of keyword objects |

### Rating Scale

- **Range:** 0.5 to 5.0
- **Steps:** 0.5 increments (10 distinct values)
- **Mean:** 3.56
- **Median:** 4.0

### Unique Counts

- **Unique users:** 162,532
- **Unique movies (with ratings):** 4,595
- **Total ratings:** 17,258,140

---

## Dataset Inspection Findings

Detailed findings from `01_dataset_exploration.ipynb`:

### Missing Values

| Column | Missing Count | Action |
|---|---|---|
| `homepage` | 2,944 | Not needed — can ignore |
| `tagline` | 727 | Not needed — can ignore |
| `overview` | 1 | Negligible — can ignore |
| All other columns | 0 | No issues |

### Duplicates

- **Full row duplicates:** 0 in all three files
- **Duplicate `tmdbId` in movies:** 4 movies appear twice (same tmdbId, different ratingId):
  - *Confessions of a Dangerous Mind* (tmdbId 4912)
  - *Pokémon: Spell of the Unknown* (tmdbId 10991)
  - *Clockstoppers* (tmdbId 15028)
  - *A Farewell to Arms* (tmdbId 22649)
- **Action taken:** Keep the entry with more ratings, drop the other

### Unrated Movies

- **7 movies** have a `ratingId` not found in the ratings file
- **Action taken:** Drop these (they have no user data)

### Table Connections

```
movies.ratingId  ←→  ratings.ratingId    (1:N — one movie, many user ratings)
movies.tmdbId    ←→  credits.tmdbId      (1:1 — movie metadata + cast/crew)
```

- 4,595 `ratingId` values exist in both movies and ratings (joinable)
- All 4,598 `tmdbId` values in movies exist in credits

### Genre Distribution

| Genre | Movie Count |
|---|---|
| Drama | ~2,000+ |
| Comedy | ~1,300+ |
| Thriller | ~1,000+ |
| Action | ~800+ |
| Romance | ~700+ |
| Horror | ~500+ |
| Adventure | ~400+ |
| Crime | ~400+ |
| Documentary | ~400+ |
| Science Fiction | ~300+ |

**20 unique genres** available for mood-to-genre mapping.

### Rating Distribution

Ratings are skewed toward 4.0 (the most common rating). This is typical for movie rating datasets — users tend to rate movies they like.

---

## Preprocessing Steps

Full implementation in `02_data_preprocessing.ipynb`. Each step is backed by established recommender-system literature.

### Step 1: Load Raw Data
- Loaded all three CSV files from `data/raw/`
- Raw files are never modified (preserved for reproducibility)

### Step 2: Drop Unrated Movies (Cold-Start Items)
- Removed 7 movies whose `ratingId` has no corresponding entries in the ratings table
- **Justification:** Items with zero interactions are *cold-start items*. Collaborative filtering requires user-item interaction history to compute similarity or learn latent factors. Items with no ratings provide no signal for any CF approach.
- **Reference:** Schein et al. (2002); Lika et al. (2014) — see References below

### Step 3: Deduplicate by tmdbId
- 4 movies appeared twice with the same `tmdbId` but different `ratingId`
- Kept the entry with the most ratings for each `tmdbId`
- **Justification:** Duplicate records violate data integrity and can bias model training. In recommender systems, duplicated item entries cause the same movie to appear multiple times in the user-item matrix, distorting similarity computations.
- **Reference:** Ilyas & Chu (2015) — see References below

### Step 4: Parse JSON Columns (Feature Extraction)
- Converted `genres` from JSON string to list: `'Comedy|Crime'` and `['Comedy', 'Crime']`
- Converted `keywords` from JSON string to list
- **Justification:** Content-based and hybrid recommender systems rely on item metadata (genres, keywords) to model item characteristics. These features must be extracted from raw storage formats into structured representations before use.
- **Reference:** Lops et al. (2010) — see References below

### Step 5: Compute Rating Statistics
- Calculated `avg_rating`, `num_ratings`, and `rating_std` per movie
- Merged into the movies table
- **Justification:** Rating frequency and value distributions significantly impact recommender system performance. Understanding these characteristics helps with filtering low-quality items and setting minimum support thresholds.
- **Reference:** Adomavicius & Zhang (2012) — see References below

### Step 6: Merge Tables
- Merged credits (cast, crew) into movies on `tmdbId`
- Enriched ratings with movie title and genres for readability
- Rationale: Denormalised dataset avoids repeated joins during algorithm development

### Step 7: Sparsity Analysis
- Computed user-item matrix dimensions and sparsity ratio
- **Justification:** Sparsity is a fundamental challenge in CF. Real-world matrices are often >99% empty, which motivates matrix factorization (SVD) over neighbourhood-based methods.
- **Reference:** Koren et al. (2009) — see References below

### Step 8: Save Processed Data
- Saved three clean CSV files to `data/processed/`

---

## Processed Output Files

| File | Rows | Columns | Contents |
|---|---|---|---|
| `movies_clean.csv` | ~4,595 | ~25 | Movie metadata + parsed genres/keywords + rating stats + credits |
| `ratings_clean.csv` | 17,258,140 | 8 | All ratings + movie title + genres |
| `movie_lookup.csv` | ~4,595 | 7 | Simple reference (ratingId, tmdbId, title, genres, avg_rating, num_ratings) |

### How to Load Processed Data

```python
import pandas as pd

movies = pd.read_csv('data/processed/movies_clean.csv')
ratings = pd.read_csv('data/processed/ratings_clean.csv')
lookup = pd.read_csv('data/processed/movie_lookup.csv')

# Note: genre_list and keyword_list are stored as strings after CSV save
# To convert back to lists:
import ast
movies['genre_list'] = movies['genre_list'].apply(ast.literal_eval)
movies['keyword_list'] = movies['keyword_list'].apply(ast.literal_eval)
```

### Getting Started (New Teammate Setup)

1. Clone this repository
2. Download the dataset from [Kaggle](https://www.kaggle.com/datasets/aayushsoni4/tmdb-5000-movie-dataset-with-ratings)
3. Place the three CSV files in `data/raw/`:
   - `tmdb_movie_dataset.csv`
   - `tmdb_movie_ratings.csv`
   - `tmdb_movie_credits.csv`
4. Run `notebook/02_data_preprocessing.ipynb` to generate `data/processed/` files
5. Load the processed data from `data/processed/`

---

## Next Steps

1. **Define mood-to-genre mapping** — agree on how moods map to genres (with justification)
2. **Implement Collaborative Filtering** (Tay Ernest)
3. **Implement Content-Based Filtering** (Chay Qian)
4. **Implement Hybrid/Other** (Member 3)
5. **Evaluate and compare** all approaches
6. **Build prototype interface**
7. **Write report sections** (methodology, results, discussion, conclusion)

---

## References

### Dataset

- [Kaggle — TMDB 5000 Movie Dataset with Ratings](https://www.kaggle.com/datasets/aayushsoni4/tmdb-5000-movie-dataset-with-ratings)
- [TMDB API](https://www.themoviedb.org/documentation/api)

### Preprocessing Justifications

1. Schein, A. I., Popescul, A., Ungar, L. H., & Pennock, D. M. (2002). Methods and metrics for cold-start recommendations. *Proceedings of the 25th Annual International ACM SIGIR Conference on Research and Development in Information Retrieval*, 253–260.
2. Lika, B., Kolomvatsos, K., & Hadjiefthymiades, S. (2014). Facing the cold start problem in recommender systems. *Expert Systems with Applications*, 41(4), 2065–2073.
3. Ilyas, I. F., & Chu, X. (2015). Trends in cleaning relational data: Consistency and deduplication. *Foundations and Trends in Databases*, 5(4), 283–399.
4. Lops, P., De Gemmis, M., & Semeraro, G. (2010). Content-based recommender systems: State of the art and trends. In P. B. (Ed.), *Recommender Systems Handbook* (pp. 73–105). Springer.
5. Adomavicius, G., & Zhang, J. (2012). Impact of data characteristics on recommender systems performance. *ACM Transactions on Management Information Systems*, 3(1), 1–23.
6. Koren, Y., Bell, R., & Volinsky, C. (2009). Matrix factorization techniques for recommender systems. *Computer*, 42(8), 30–37.
